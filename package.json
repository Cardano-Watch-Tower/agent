{
  "name": "@cardanowatchtower/agent",
  "version": "0.1.0",
  "description": "Cardano blockchain monitoring agent — watches large movements, answers queries, reports findings",
  "main": "src/index.js",
  "scripts": {
    "watch": "node src/watcher.js",
    "test": "node src/watcher.js --test"
  },
  "dependencies": {
    "@blockfrost/blockfrost-js": "^6.0.0",
    "dotenv": "^17.0.0",
    "nodemailer": "^8.0.2",
    "puppeteer": "^24.38.0"
  },
  "devDependencies": {
    "puppeteer-core": "^24.38.0"
  }
}
